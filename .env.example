SPRING_PROFILES_ACTIVE=prod

SERVER_PORT=8081
SERVER_CONTEXT_PATH=/
BACKEND_EXTERNAL_PORT=8090

SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/vacation_db
SPRING_DATASOURCE_USERNAME=vacation_user
SPRING_DATASOURCE_PASSWORD=change_me
SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE=30
SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE=10

POSTGRES_DB=vacation_db
POSTGRES_USER=vacation_user
POSTGRES_PASSWORD=change_me

# Only for local development with docker-compose.local.yml
POSTGRES_EXTERNAL_PORT=5433

APP_JWT_SECRET=change_me_to_a_long_random_secret
APP_JWT_EXPIRATION=86400000
APP_CORS_ALLOWED_ORIGIN_PATTERNS=https://your-frontend.example.com
APP_OPENAPI_LOCAL_URL=http://localhost:8090
APP_OPENAPI_PROD_URL=https://api.example.com
APP_STORAGE_AVATAR_DIR=/app/uploads/avatars
APP_STORAGE_TRIP_EVENT_IMAGE_DIR=/app/uploads/trip-events
APP_DOCUMENTS_ORGANIZATION_NAME=АО "Национальные информационные технологии"
APP_BIOMETRIC_PROVIDER_NAME=mock-face-provider
# Production must use APP_BIOMETRIC_PROVIDER=http. The mock provider is only for local development/demo.
APP_BIOMETRIC_PROVIDER=http
APP_BIOMETRIC_HTTP_BASE_URL=https://biometric.example.com
APP_BIOMETRIC_HTTP_API_KEY=change_me
FIREBASE_SECRETS_DIR=/opt/secrets
FIREBASE_SERVICE_ACCOUNT_PATH=/opt/secrets/firebase-service-account.json
# Alternative if you prefer storing the JSON in env:
# FIREBASE_SERVICE_ACCOUNT_BASE64=

# Optional only if you explicitly run the containerized nginx profile:
# docker compose --profile proxy up -d
NGINX_HTTP_PORT=8090
